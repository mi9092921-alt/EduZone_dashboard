# Dependencies
node_modules/

# Next.js
.next/
out/

# Turbo
.turbo/

# Build outputs
dist/

# Environment variables
.env
.env.local
.env.test
.env.development.local
.env.test.local
.env.production.local
.env*.local
scripts/.env.deploy

# Testing
coverage/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Debug
npm-debug.log*
pnpm-debug.log*

# Supabase
supabase/.temp/
supabase/db_url.txt
supabase/db_url.test.txt

*storybook.log
storybook-static
*.patch
*.diff
*.tsbuildinfo
*.log
apps/admin/tsconfig.tsbuildinfo
packages/types/tsconfig.tsbuildinfo
packages/ui/tsconfig.tsbuildinfo

# Playwright / dev-server run artifacts (contain local run output, stack
# traces, and screenshots -- one committed error-context.md leaked a seeded
# e2e test credential; these are regenerated on every run and must never be
# tracked)
apps/admin/playwright-report/
apps/admin/test-results/
apps/admin/next-dev-log.txt
err.html
apps/admin/err.html
# Saved browser storage state -- contains live session JWTs by definition
apps/admin/playwright/.auth/
# Ad-hoc downloaded/extracted Playwright report archives (full traces with
# screenshots, network logs and session JWTs -- security review item 06:
# pw*/report.zip were committed carrying local-stack Bearer tokens)
pw-report-dl/
pw2/
pw3/
**/report.zip