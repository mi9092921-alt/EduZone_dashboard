# Copy to scripts/.env.deploy (gitignored) or export before deploy/audit.
# Option A — Supabase Management API (recommended with MCP / dashboard PAT)
SUPABASE_ACCESS_TOKEN=sbp_...
SUPABASE_PROJECT_REF=vlezaxucklrwiouoamkk

# Option B — Direct Postgres (Settings → Database → connection string)
# DATABASE_URL=postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
